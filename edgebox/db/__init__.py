"""Shared DB identifiers (``mongo_collections``) and query helpers (``mission_queries``).

应用启动时应在导入其它业务模块之前执行 ``import Utils.edgebox_repo``（各产品 ``Utils/edgebox_repo.py``），
以便解析 ``edgebox.config`` / ``edgebox.db``。
"""
